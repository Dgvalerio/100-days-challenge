import { NextPage } from 'next';
import { Inter } from 'next/font/google';
import Head from 'next/head';
import { useRouter } from 'next/router';

import { CssBaseline, Typography } from '@mui/material';

import styled from '@emotion/styled';

import { Day001 } from '@/pages/001';
import { Day002 } from '@/pages/002';

const inter = Inter({ subsets: ['latin'] });

export const Tasks = styled.main`
  display: flex;
  justify-content: space-around;
  padding: 1rem;

  div {
    cursor: pointer;
    text-align: center;
  }
`;

const Home: NextPage = () => {
  const router = useRouter();

  return (
    <>
      <CssBaseline />
      <Head>
        <title>100 Days Challenge</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className={inter.className}>
        <Typography variant="h2" textAlign="center">
          100 Days Challenge
        </Typography>
        <Tasks>
          <div onClick={(): void => void router.push('/001')}>
            <Typography variant="overline">Day 001</Typography>
            <Day001 />
          </div>
          <div onClick={(): void => void router.push('/002')}>
            <Typography variant="overline">Day 002</Typography>
            <Day002 />
          </div>
        </Tasks>
      </main>
    </>
  );
};

export default Home;
